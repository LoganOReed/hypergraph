# Example python file for reference


def succ(num):
    """A one-line summary that does not use variable names.

    Several Sentences providing an extended description. Reference
    variables using back-ticks, e.g. `num`

    Parameters
    ----------
    num : int
        A description of num

    Returns
    -------
    out : int
        An explanation of `out`. `out` = `num` + 1.
    """
    return num + 1
